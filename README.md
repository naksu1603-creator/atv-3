# Entrega III — SPA JavaScript (ONG RAGRID)

Projeto de exemplo para a disciplina — SPA simples com validação de formulários, templates e localStorage.

## Como usar

1. Baixe ou clone o repositório.
2. Abra `index.html` em um navegador ou rode via servidor local (recomendado):

```bash
python -m http.server 5500
# acesse http://localhost:5500
